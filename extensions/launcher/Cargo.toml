[package]
name = "launcher"
version = "0.1.0"
edition = "2021"

[dependencies]
egui = "0.36"
eframe = { version = "0.36", default-features = false, features = ["glow", "x11", "wayland"] }
dotenv = "0.15"
reqwest = { version = "0.11", features = ["blocking", "json"] }
serde_json = "1.0"

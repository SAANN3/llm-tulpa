.claude/
.DS_Store
.env

# design-sync (/design-sync skill) — this project doesn't want any of its output
# (config, previews, design-handoff templates) published or tracked at all, so the
# whole thing is ignored rather than the usual narrower "keep the durable state"
# pattern.
.ds-sync/
ds-bundle/
.design-sync/
design_handoff_*/
frontend/src/design-sync-preview.css
